"""llm — the from-scratch language-model library for the course.

Import path: run scripts from ``course/`` (labs add it to ``sys.path`` automatically).

    from llm.config import preset
    from llm.model import TinyLM
    from llm.tokenizer import BPETokenizer
"""
__version__ = "1.0.0"
